<h2>My Bookings</h2>

<%  @bookings.each do |booking| %>
   <p><%= booking.date_time.strftime("#{booking.date_time.day.ordinalize} %B %Y") %></p>
   <p><%= booking.date_time.strftime("%H:%M") %></p>
  <%= booking.number_of_people %>
<% end %>
